server {
    listen      80;
    
    location /uploads/ {
        root /home/box/web/uploads;
    }
    
    location ~* ^.+\.[a-z0-9A-Z_]+ {
        root /home/box/web/public;
    }
    
    location / {
        return 404;
    }
}

